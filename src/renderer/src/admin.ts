// Hardcoded on purpose: this is a visitor deterrent on a kiosk with no network and no
// user accounts, not a real security boundary (see ARCHITECTURE.md "Stack").
export const ADMIN_PASSWORD = 'voces2024'
