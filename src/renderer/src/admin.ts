// Digits only: the kiosk ships without a keyboard, so this is entered on the on-screen keypad.
// Hardcoded on purpose — a visitor deterrent on a machine with no network and no user accounts,
// not a real security boundary (see ARCHITECTURE.md "Stack").
export const ADMIN_PIN = '2024'
